# Prompt Notes

This repository follows `MISSION_1.md` and is currently scoped to Milestone 1 and Milestone 2 only.

Implementation rules:

- Keep the first working version simple and testable.
- Do not hardcode dataset field names if configuration can express them.
- Put pipeline logic in `src/`, not in `scripts/`.
- Record unresolved schema assumptions in the README TODO section.
